---
layout: default
title: A hint of chaos
---

- var posts = items.map(function(p) {
-   p.myDate = new Date(p.meta.date);
-   return p;
- }).sort(function(p1, p2) {
-   return p2.myDate.getTime() - p1.myDate.getTime()
- }).filter(function(p) {
-   return !p.meta.hidden;
- });

each post in posts.slice(0,5)
    .post
        h1
            a(href=post.url)=post.meta.title
        .date= post.myDate.toDateString()
        .content!=post.body
        .comments
            hr
            a(href=post.url + '#_comments') comment or share

- var older = posts.slice(5);
if (older.length)
    hr
    h1 Older posts
    each post in older
        h4.post
            a(href=post.url)=post.meta.title

